export const add = (a, b) => a + b;
export const subtract = (a, b) => a - b;

// You can also have a default export alongside named exports
export default function multiply(a, b) {
    return a * b;
} 